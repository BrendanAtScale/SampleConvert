unique_name: .EAC Display Estimate
object_type: metric_calc
label: .EAC Display Estimate
expression: >-
  0 /*CALCULATE([Estimate Count], FILTER(VALUES(Projects[Project Start Date]),
  Projects[Project Start Date] <= MAX(Dates[Date])),
  FILTER(VALUES(Projects[Project End Date]), Projects[Project End Date] >=
  MIN(Dates[Date])) ) > 0 TODO: Update with valid MDX expression*/
folder: Estimate
is_hidden: true
