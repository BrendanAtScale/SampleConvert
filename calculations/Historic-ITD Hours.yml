unique_name: Historic-ITD Hours
object_type: metric_calc
label: Historic-ITD Hours
expression: >-
  0 /*if(NOT([IsTaskLevelFiltered]) && NOT([IsResourceFiltered]) &&
  [DateFiltered],
  CALCULATE(sum('Historic-PSR'[TOTAL_LABOR_HOURS_ITD]),LASTNONBLANK('Dates'[Date],CALCULATE(Sum('Historic-PSR'[TOTAL_LABOR_HOURS_ITD])))))
  TODO: Update with valid MDX expression*/
format: '#,0.00'
is_hidden: true
