unique_name: Historic-ETC Revenue
object_type: metric_calc
label: Historic-ETC Revenue
expression: >-
  0 /*if(NOT([IsTaskLevelFiltered]) && NOT([IsResourceFiltered]) &&
  [HasOneCurrency] && [DateFiltered],
  CALCULATE(sum('Historic-PSR'[ETC_REVENUE]),LASTNONBLANK('Dates'[Date],CALCULATE(Sum('Historic-PSR'[ETC_REVENUE])))))
  TODO: Update with valid MDX expression*/
folder: Estimate to Complete (ETC)
format: '""#,0.00;(""#,0.00);""#,0.00'
