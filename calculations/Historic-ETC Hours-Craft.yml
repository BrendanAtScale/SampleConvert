unique_name: Historic-ETC Hours-Craft
object_type: metric_calc
label: Historic-ETC Hours-Craft
expression: >-
  0 /*if(NOT([IsTaskLevelFiltered]) && NOT([IsResourceFiltered]) &&
  [DateFiltered],
  CALCULATE(sum('Historic-PSR'[Historic-ETC-Hours-Craft]),LASTNONBLANK('Dates'[Date],CALCULATE(Sum('Historic-PSR'[Historic-ETC-Hours-Craft])))))
  TODO: Update with valid MDX expression*/
folder: Estimate to Complete (ETC)
format: '#,0.00'
